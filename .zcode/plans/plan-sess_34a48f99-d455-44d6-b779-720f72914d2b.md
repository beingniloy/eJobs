## CV Builder Full Debug Audit — 12 Issues Found

### 🔴 CRITICAL (Data Loss)

**Bug 1: `references` & `training` missing from livePreview**
`CvResumeController.php:376` — `livePreview()` only passes 10 keys to renderer. `references` and `training` stripped. Inline editor preview NEVER shows these sections.

**Bug 2: `references` & `training` missing from resume snapshot**
`CvResumeController.php:154-165` — `create()` data_snapshot doesn't include them. PDF downloads from created resumes permanently lose references/training.

**Bug 3: CvPreviewClient shows demo data instead of user's CV**
`CvPreviewClient.tsx:61` — Calls `getLivePreview(slug)` (GET, no snapshot data) instead of `renderPreview(uuid)` which reads from the resume's frozen snapshot.

### 🟡 MEDIUM

**Bug 4: `sanitizeForBlade` duplicated** in `use-resume-editor.ts` AND `edit/[slug]/page.tsx`
**Bug 5: Inline editor hangs on backend preview failure** — No error state, shows "Loading..." forever
**Bug 6: Wizard sends `custom_blocks`** but we removed that column from DB
**Bug 7: AI prompt JSON schema missing `references` & `training`**

### 🟢 MINOR
- `buildClientPreview` regex fragile (acceptable as fallback)
- PDF depends on Chrome installed (works in dev)
- DomPDF fallback loses Google Fonts

### Fix Plan (7 files, ~30 lines changed)

| # | File | Fix |
|---|------|-----|
| 1 | `CvResumeController.php` | Add `references`, `training` to `livePreview()` allowed keys + `create()` data_snapshot + AI prompt |
| 2 | `CvPreviewClient.tsx` | Use `renderPreview(uuid)` for created resumes |
| 3 | `InlineEditor.tsx` | Add error state + client preview fallback |
| 4 | `TemplateStep.tsx` | Remove `custom_blocks` from wizard data mapping |
| 5 | `cv-builder-utils.ts` | Add shared `sanitizeForBlade` |
| 6 | `use-resume-editor.ts` | Import from utils, remove duplicate |
| 7 | `edit/[slug]/page.tsx` | Import from utils, remove duplicate |